import { $ } from "../../utils/dom.js";

let $startLineSelector = $("#line-start-station-selector");
let $endLineSelector = $("#line-end-station-selector");
export function initRenderLine() {
  console.log($startLineSelector);
}
